# 📸 Capturas de Pantalla

Este directorio contiene las capturas de pantalla de la aplicación "Control de Horario".

## Nombres de archivos esperados:

- `dashboard.png` - Panel principal/dashboard
- `fichajes.png` - Control de fichajes
- `administracion.png` - Panel administrativo
- `solicitudes.png` - Gestión de solicitudes

## Instrucciones para agregar capturas:

1. Tomar capturas de pantalla de cada pantalla principal
2. Guardarlas con nombres descriptivos en este directorio
3. Asegurar que sean imágenes PNG o JPG
4. Tamaño recomendado: 1280x720px (16:9)

---

**Nota:** Los archivos de imagen no son versionados por defecto (ver `.gitignore`).
Para incluirlos en el repositorio, usar:
```bash
git add -f docs/screenshots/*.png
git commit -m "docs: agregar capturas de pantalla"
```
